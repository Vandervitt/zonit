# SEO 编辑面板 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让用户每页可覆盖 SEO 元数据（标题/描述/分享图/noindex），覆盖优先、hero 派生兜底，并落到公开页 `<head>` 与 robots.txt。

**Architecture:** schema 加 `seo?`；纯函数 `resolvePageMeta` 统一"覆盖优先、派生兜底"；`generateMetadata` 与 `robots.ts` 复用之；编辑器加 SEO 配置面板（页面级常驻件，完全类比 branding）。

**Tech Stack:** Next.js 16(App Router)、TypeScript、Tailwind、vitest、Playwright。

设计来源:`docs/superpowers/specs/2026-06-26-seo-panel-design.md`

---

## File Structure

- `types/schema.draft.ts` —（改）`PageSeo` + `LandingPageDraft.seo?`。
- `lib/seo/resolve.ts` —（新）`resolvePageMeta` + `heroOgImage`（从 page.tsx 迁入）。
- `lib/seo/resolve.test.ts` —（新）单测。
- `app/p/[slug]/page.tsx` —（改）generateMetadata 用 resolvePageMeta + noindex。
- `app/robots.ts` —（改）自有域读 seo.noindex → disallow。
- `landing-editor/store/defaults.ts` —（改）`createSeo`。
- `landing-editor/store/editorStore.tsx` —（改）state/action/toDraft 接入 seo。
- `landing-editor/sampleDraft.ts` —（改）fromDraft 读入 seo。
- `landing-editor/forms/SeoForm.tsx` —（新）配置面板。
- `landing-editor/components/BlockList.tsx` —（改）SEO 常驻入口。
- `landing-editor/components/EditorDetail.tsx` —（改）渲染 SeoForm。
- `e2e/seo.spec.ts` —（新）e2e。

---

## Task 1: schema PageSeo

**Files:**
- Modify: `types/schema.draft.ts`

- [ ] **Step 1: 加类型**

在 `types/schema.draft.ts` 中 `LandingPageDraft` 之前加：
```ts
/** 页面 SEO 覆盖（留空回退首屏派生）。 */
export interface PageSeo {
  metaTitle?: string;
  metaDescription?: string;
  ogImage?: string;
  noindex?: boolean;
}
```
在 `LandingPageDraft` 接口加（与 `branding?` 同级）：
```ts
  seo?: PageSeo;
```

- [ ] **Step 2: 类型检查**

Run: `npx tsc --noEmit`
Expected: 无输出

- [ ] **Step 3: 提交**

```bash
git add types/schema.draft.ts
git commit -m "feat: schema 新增 PageSeo"
```

---

## Task 2: resolvePageMeta 纯函数（TDD）

**Files:**
- Create: `lib/seo/resolve.ts`
- Test: `lib/seo/resolve.test.ts`

- [ ] **Step 1: 写失败测试**

`lib/seo/resolve.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { resolvePageMeta } from "./resolve";
import type { LandingPageDraft } from "@/types/schema.draft";

function draft(over: Partial<LandingPageDraft> = {}): LandingPageDraft {
  return {
    hero: {
      title: "首屏标题\n换行", subtitle: "首屏副标题",
      cta: { text: "咨询", link: "" },
      backgroundImage: { src: "https://img/bg.jpg" },
    },
    sections: [],
    footer: { brandName: "Brand", copyrightYear: "2026", contactEmail: "", privacyPolicy: "", termsOfService: "" },
    ...over,
  } as LandingPageDraft;
}

describe("resolvePageMeta", () => {
  it("无 seo：回退 hero（title 去换行、desc=subtitle、og=背景图、noindex=false）", () => {
    const r = resolvePageMeta(draft());
    expect(r.title).toBe("首屏标题 换行");
    expect(r.description).toBe("首屏副标题");
    expect(r.ogImage).toBe("https://img/bg.jpg");
    expect(r.noindex).toBe(false);
  });
  it("seo 覆盖：用覆盖值", () => {
    const r = resolvePageMeta(draft({ seo: { metaTitle: "自定义标题", metaDescription: "自定义描述", ogImage: "https://img/og.jpg", noindex: true } }));
    expect(r.title).toBe("自定义标题");
    expect(r.description).toBe("自定义描述");
    expect(r.ogImage).toBe("https://img/og.jpg");
    expect(r.noindex).toBe(true);
  });
  it("seo 字段空白：回退 hero", () => {
    const r = resolvePageMeta(draft({ seo: { metaTitle: "   ", metaDescription: "" } }));
    expect(r.title).toBe("首屏标题 换行");
    expect(r.description).toBe("首屏副标题");
  });
  it("showcase 图作 og 兜底（无背景图）", () => {
    const d = draft();
    delete (d.hero as { backgroundImage?: unknown }).backgroundImage;
    d.hero.showcase = { type: "image", src: "https://img/show.jpg" };
    expect(resolvePageMeta(d).ogImage).toBe("https://img/show.jpg");
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run lib/seo/resolve.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 写实现**

`lib/seo/resolve.ts`:
```ts
// lib/seo/resolve.ts
// 页面元数据解析：seo 覆盖优先，hero 派生兜底。
import type { LandingPageDraft, HeroSection } from "@/types/schema.draft";

export interface ResolvedMeta {
  title: string;
  description: string;
  ogImage?: string;
  noindex: boolean;
}

/** 从首屏派生 OG 图：优先背景图，其次展示图（视频取封面）。 */
export function heroOgImage(hero: HeroSection): string | undefined {
  if (hero.backgroundImage?.src) return hero.backgroundImage.src;
  if (hero.showcase?.type === "image") return hero.showcase.src;
  if (hero.showcase?.type === "video") return hero.showcase.poster;
  return undefined;
}

const nonBlank = (v?: string): string | undefined => {
  const t = v?.trim();
  return t ? t : undefined;
};

export function resolvePageMeta(data: LandingPageDraft): ResolvedMeta {
  const { hero, seo } = data;
  return {
    title: nonBlank(seo?.metaTitle) ?? hero.title.replace(/\n/g, " "),
    description: nonBlank(seo?.metaDescription) ?? hero.subtitle ?? "",
    ogImage: nonBlank(seo?.ogImage) ?? heroOgImage(hero),
    noindex: seo?.noindex === true,
  };
}
```
> 注：`hero.subtitle` 若 schema 中可选，则 `?? ""` 兜底；若必填则去掉 `?? ""`。以 `HeroSection` 实际定义为准（实现时确认 subtitle 是否可选并对齐）。

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run lib/seo/resolve.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add lib/seo/resolve.ts lib/seo/resolve.test.ts
git commit -m "feat: SEO resolvePageMeta 纯函数（覆盖优先、hero 兜底）"
```

---

## Task 3: generateMetadata 接入 resolvePageMeta

**Files:**
- Modify: `app/p/[slug]/page.tsx`

- [ ] **Step 1: 改 generateMetadata**

在 `app/p/[slug]/page.tsx`：删除本地 `heroOgImage` 函数（移至 lib/seo），import 改为：
```ts
import { resolvePageMeta } from "@/lib/seo/resolve";
```
（同时删掉不再用的 `import type { HeroSection }`，若它处仍用则保留。）
把 `generateMetadata` 内构造 title/description/ogImage 的部分替换为：
```ts
  const { title, description, ogImage, noindex } = resolvePageMeta(page.data);

  const host = hostnameOf((await headers()).get("host"));
  const canonical = `https://${host}/`;

  return {
    title,
    description,
    alternates: { canonical },
    icons: page.data.branding?.favicon ? { icon: page.data.branding.favicon } : undefined,
    robots: noindex ? { index: false, follow: false } : undefined,
    openGraph: {
      type: "website",
      url: canonical,
      siteName: footer.brandName,
      title,
      description,
      images: ogImage ? [{ url: ogImage }] : undefined,
    },
    twitter: {
      card: ogImage ? "summary_large_image" : "summary",
      title,
      description,
      images: ogImage ? [ogImage] : undefined,
    },
  };
```
> 保留原有 `const { hero, footer } = page.data;` 中 `footer`（siteName 用）；`hero` 若不再直接用可移除解构。以 tsc 为准清理未用变量。

- [ ] **Step 2: 类型检查 + lint**

Run: `npx tsc --noEmit && npx eslint "app/p/[slug]/page.tsx"`
Expected: 均无错误

- [ ] **Step 3: 提交**

```bash
git add "app/p/[slug]/page.tsx"
git commit -m "feat: 公开页 generateMetadata 接入 SEO 覆盖 + noindex"
```

---

## Task 4: robots.txt noindex 分支

**Files:**
- Modify: `app/robots.ts`

- [ ] **Step 1: 改 robots 自有域分支**

把 `app/robots.ts` 替换为：
```ts
import type { MetadataRoute } from "next";
import { headers } from "next/headers";
import { hostnameOf, isCustomDomain } from "@/lib/host";
import { getLandingSlugByCustomDomain } from "@/lib/domains-db";
import { getPublishedBySlug } from "@/lib/landing-pages/store";

// 按访问 host 动态生成：读 header 使其成为每请求动态路由（绕过默认缓存）。
export default async function robots(): Promise<MetadataRoute.Robots> {
  const hostname = hostnameOf((await headers()).get("host"));

  // 租户自有域名：仅托管一张已发布落地页。
  if (isCustomDomain(hostname)) {
    const slug = await getLandingSlugByCustomDomain(hostname);
    const page = slug ? await getPublishedBySlug(slug) : null;
    // 该页 noindex → 禁收录整站（自有域单页），不输出 sitemap。
    if (page?.data.seo?.noindex) {
      return { rules: { userAgent: "*", disallow: "/" } };
    }
    return {
      rules: { userAgent: "*", allow: "/" },
      sitemap: `https://${hostname}/sitemap.xml`,
    };
  }

  // 平台主域：放开营销面（/、/pricing），禁后台与接口。
  return {
    rules: { userAgent: "*", allow: "/", disallow: ["/admin", "/super-admin", "/api"] },
  };
}
```

- [ ] **Step 2: 类型检查 + lint**

Run: `npx tsc --noEmit && npx eslint app/robots.ts`
Expected: 均无错误

- [ ] **Step 3: 提交**

```bash
git add app/robots.ts
git commit -m "feat: robots.txt 按页面 noindex 禁收录（自有域）"
```

---

## Task 5: 编辑器 store 接入 seo

**Files:**
- Modify: `landing-editor/store/defaults.ts`
- Modify: `landing-editor/store/editorStore.tsx`
- Modify: `landing-editor/sampleDraft.ts`

- [ ] **Step 1: 默认值工厂**

`landing-editor/store/defaults.ts`：类型 import 加 `PageSeo`；在 `createBranding` 之后加：
```ts
export const createSeo = (): PageSeo => ({});
```

- [ ] **Step 2: store 接入**

`landing-editor/store/editorStore.tsx`：
1. 类型 import 加 `PageSeo`；`import { ..., createSeo } from "./defaults";`（与 createBranding 同处）。
2. 常量加：`export const SEO_ID = "seo";`
3. `EditorState` 加：`seo: PageSeo;`
4. `EditorAction` 加：`| { kind: "updateSeo"; value: PageSeo }`
5. reducer 加 case（branding 附近）：
```ts
    case "updateSeo":
      return { ...state, seo: action.value };
```
6. `toDraft` 加（`draft.branding = state.branding;` 附近）：`draft.seo = state.seo;`

- [ ] **Step 3: fromDraft 读入**

`landing-editor/sampleDraft.ts`：import 加 `createSeo`（从 `./store/defaults`）；返回对象加：
```ts
    seo: draft.seo ?? createSeo(),
```

- [ ] **Step 4: 类型检查**

Run: `npx tsc --noEmit`
Expected: 无输出（若有缺 seo 的 EditorState 构造点报错，按提示补 `seo: ... ?? createSeo()`）

- [ ] **Step 5: 提交**

```bash
git add landing-editor/store/defaults.ts landing-editor/store/editorStore.tsx landing-editor/sampleDraft.ts
git commit -m "feat: 编辑器 store 接入 seo（常驻，默认空）"
```

---

## Task 6: SeoForm 面板 + 入口

**Files:**
- Create: `landing-editor/forms/SeoForm.tsx`
- Modify: `landing-editor/components/EditorDetail.tsx`
- Modify: `landing-editor/components/BlockList.tsx`

- [ ] **Step 1: 新建面板**

`landing-editor/forms/SeoForm.tsx`:
```tsx
"use client";
// landing-editor/forms/SeoForm.tsx
// 页面 SEO 配置：标题/描述/分享图/noindex（留空回退首屏派生）。
import type { PageSeo } from "@/types/schema.draft";
import { Field } from "../ui/Field";
import { TextInput } from "../ui/TextInput";
import { TextArea } from "../ui/TextArea";
import { MediaPicker } from "../ui/MediaPicker";

export function SeoForm({ value, onChange }: { value: PageSeo; onChange: (v: PageSeo) => void }) {
  const patch = (p: Partial<PageSeo>) => onChange({ ...value, ...p });
  return (
    <div className="space-y-3">
      <Field label="页面标题（留空用首屏标题，建议 ≤60 字）">
        <TextInput value={value.metaTitle ?? ""} onChange={(e) => patch({ metaTitle: e.target.value || undefined })} placeholder="搜索结果 / 浏览器标签显示的标题" />
      </Field>
      <Field label="页面描述（留空用首屏副标题，建议 ≤160 字）">
        <TextArea value={value.metaDescription ?? ""} onChange={(e) => patch({ metaDescription: e.target.value || undefined })} placeholder="搜索结果摘要 / 社交分享描述" />
      </Field>
      <Field label="社交分享图（留空用首屏图）">
        <MediaPicker value={value.ogImage ?? ""} accept="image" onChange={(src) => patch({ ogImage: src || undefined })} />
      </Field>
      <label className="flex items-center gap-2 text-sm text-ink-soft">
        <input
          type="checkbox"
          checked={value.noindex ?? false}
          onChange={(e) => patch({ noindex: e.target.checked || undefined })}
          className="h-3.5 w-3.5 rounded border-edge-strong text-brand-600 focus:ring-brand-500/30"
        />
        禁止搜索引擎收录此页（noindex）
      </label>
    </div>
  );
}
```
> 注：确认 `landing-editor/ui/TextArea` 存在且 props 为 `{value,onChange}`（与 TextInput 一致）。若不存在或签名不同，按实际 UI 组件调整（可改用 TextInput 或现有多行组件）。

- [ ] **Step 2: EditorDetail 渲染**

`landing-editor/components/EditorDetail.tsx`：import 加 `import { SeoForm } from "../forms/SeoForm";` 与从 store 的 `SEO_ID`。在 `BRANDING_ID` 分支之后加：
```tsx
  } else if (id === SEO_ID) {
    title = "SEO";
    body = <SeoForm value={state.seo} onChange={(v) => dispatch({ kind: "updateSeo", value: v })} />;
```

- [ ] **Step 3: BlockList 加入口**

`landing-editor/components/BlockList.tsx`：从 store import 加 `SEO_ID`。参照"品牌主题" `FixedRow`，在其后加：
```tsx
        <FixedRow
          label="SEO"
          hint="标题 / 描述 / 分享图"
          selected={state.selectedId === SEO_ID}
          onSelect={() => dispatch({ kind: "select", id: SEO_ID })}
        />
```
> `FixedRow` props 以该文件"品牌主题"用法为准（label/hint/selected/onSelect）。

- [ ] **Step 4: 类型检查 + lint**

Run: `npx tsc --noEmit && npx eslint landing-editor/forms/SeoForm.tsx landing-editor/components/EditorDetail.tsx landing-editor/components/BlockList.tsx`
Expected: 均无错误

- [ ] **Step 5: 提交**

```bash
git add landing-editor/forms/SeoForm.tsx landing-editor/components/EditorDetail.tsx landing-editor/components/BlockList.tsx
git commit -m "feat: 编辑器 SEO 配置面板 + 入口"
```

---

## Task 7: e2e + 全量验证

**Files:**
- Create: `e2e/seo.spec.ts`

- [ ] **Step 1: 写 e2e**

参考 `e2e/landing-pages-flow.spec.ts` 的自有域 fixture 方式（pg 直插已发布页 + 自定义域名映射），新建 `e2e/seo.spec.ts`：
```ts
// e2e/seo.spec.ts
// SEO：已发布页带自定义 seo（metaTitle/metaDescription/noindex）→ 自有域公开页 head 反映 + robots.txt disallow。
// 用 pg 直插一张带 seo 的已发布页 + 自定义域名映射（参考 landing-pages-flow.spec.ts）。
import { test, expect, request as pwRequest } from "@playwright/test";
import { Pool } from "pg";
import { config as loadEnv } from "dotenv";

loadEnv({ path: ".env.local" });
loadEnv({ path: ".env" });

const RUN = process.env.RUN_DB_E2E === "1";
const BASE = "http://localhost:3001";
const HOST = "seo-e2e.example.com";

function makePool(): Pool {
  const cs = process.env.E2E_DATABASE_URL ?? process.env.DATABASE_URL ?? "";
  const isLocal = cs.includes("localhost") || cs.includes("127.0.0.1");
  return new Pool({ connectionString: cs, ssl: isLocal ? false : { rejectUnauthorized: false } });
}

let pool: Pool;
let userId: string;
let pageId: string;
const SLUG = `e2e-seo-${Date.now()}`;

const draft = {
  hero: { title: "首屏标题", subtitle: "首屏副标题", cta: { text: "x", link: "" } },
  sections: [],
  footer: { brandName: "Brand", copyrightYear: "2026", contactEmail: "", privacyPolicy: "", termsOfService: "" },
  seo: { metaTitle: "SEO自定义标题", metaDescription: "SEO自定义描述", noindex: true },
};

test.describe("SEO 元数据", () => {
  test.skip(!RUN, "Set RUN_DB_E2E=1 to run database-backed e2e tests.");

  test.beforeAll(async () => {
    pool = makePool();
    const u = await pool.query(
      `INSERT INTO users (email, name, plan) VALUES ('seo-e2e@localhost','SEO E2E','pro')
       ON CONFLICT (email) DO UPDATE SET plan='pro' RETURNING id`);
    userId = u.rows[0].id;
    const p = await pool.query(
      `INSERT INTO landing_pages (user_id, name, data, slug, status, published_at)
       VALUES ($1, 'SEO 页', $2::jsonb, $3, 'published', NOW()) RETURNING id`,
      [userId, JSON.stringify(draft), SLUG]);
    pageId = p.rows[0].id;
    // 自定义域名映射（表/列名以 domains-db 实际为准；参考 landing-pages-flow.spec.ts fixture）
    await pool.query(
      `INSERT INTO domains (user_id, landing_page_id, hostname, status)
       VALUES ($1, $2, $3, 'verified') ON CONFLICT DO NOTHING`,
      [userId, pageId, HOST]);
  });

  test.afterAll(async () => {
    if (pageId) await pool.query(`DELETE FROM domains WHERE landing_page_id = $1`, [pageId]);
    if (userId) await pool.query(`DELETE FROM landing_pages WHERE user_id = $1`, [userId]);
    if (userId) await pool.query(`DELETE FROM users WHERE id = $1`, [userId]);
    await pool.end();
  });

  test("公开页 head 用自定义 SEO + noindex", async () => {
    const api = await pwRequest.newContext();
    const res = await api.get(`${BASE}/`, { headers: { host: HOST } });
    expect(res.status()).toBe(200);
    const html = await res.text();
    expect(html).toContain("SEO自定义标题");
    expect(html).toContain("SEO自定义描述");
    expect(html.toLowerCase()).toContain("noindex");
    await api.dispose();
  });

  test("robots.txt 对 noindex 页 disallow", async () => {
    const api = await pwRequest.newContext();
    const res = await api.get(`${BASE}/robots.txt`, { headers: { host: HOST } });
    const body = await res.text();
    expect(body).toMatch(/Disallow:\s*\//);
    await api.dispose();
  });
});
```
> 注：domains 表/列名（hostname/status/landing_page_id）以 `lib/domains-db.ts` 与 `landing-pages-flow.spec.ts` 实际 fixture 为准；实现时对齐其插入方式（这是本计划唯一需读既有 e2e 对齐的点）。

- [ ] **Step 2: 确保 DB + 迁移 + seed**

Run: `docker exec zapbridge-pg-dev pg_isready -U postgres -d zapbridge && pnpm migrate:up && pnpm db:seed-dev`
Expected: migrations complete + seed 成功

- [ ] **Step 3: 跑该 e2e**

Run: `lsof -ti:3001 | xargs kill 2>/dev/null; RUN_DB_E2E=1 pnpm exec playwright test e2e/seo.spec.ts`
Expected: 2 passed（若 domains fixture 字段不符，按 landing-pages-flow.spec.ts 实际对齐后跑通）

- [ ] **Step 4: 全量验证**

Run: `npx tsc --noEmit && npx eslint . && npx vitest run && RUN_DB_E2E=1 pnpm test:e2e`
Expected: tsc 通过；eslint 0 error；vitest 全绿（含 resolvePageMeta）；e2e 全 passed（原 12 + 新 2）。
（`npx next build` 若因 Google 字体网络不可达失败，属环境问题，单独说明。）

- [ ] **Step 5: 提交**

```bash
git add e2e/seo.spec.ts
git commit -m "test(e2e): SEO 自定义元数据 + noindex robots"
```

---

## 验收标准（对照 spec）

- schema 有 `seo?`（metaTitle/metaDescription/ogImage/noindex）。
- `resolvePageMeta` 覆盖优先、hero 派生兜底（含单测）。
- 公开页 `generateMetadata` 用 resolvePageMeta，noindex → `<meta robots noindex>`。
- robots.txt 自有域 noindex → `disallow:/` 不出 sitemap。
- 编辑器有"SEO"常驻入口 + 面板（标题/描述/分享图 MediaPicker/noindex）。
- e2e 覆盖公开页 head 自定义 + robots disallow。
- tsc / eslint / vitest / e2e 全绿。
