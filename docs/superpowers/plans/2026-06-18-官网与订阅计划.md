# 官网 + 路由分层 + 订阅计划 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `/` 成为公开营销首页、把租户后台与平台后台分层到独立路由前缀、把订阅计划收敛成单一数据源并真正强制限额、在官网与 admin 两端一致展示套餐。

**Architecture:** 三阶段顺序推进。阶段1 是行为不变的路由搬家（租户→`/admin/*`、编辑器→`/admin/editor/*`、平台→`/super-admin/*`），用路由组 `(workspace)` 隔离 Sidebar 壳与全屏编辑器。阶段2 把套餐数据收敛进 `lib/plans.ts` 单一源、抽共享展示组件、在创建落地页端强制 `landingPagesLimit`。阶段3 在 `/` 建营销首页复用套餐组件并修正平台域 robots。

**Tech Stack:** Next.js 16 App Router（Server Components）、NextAuth（proxy.ts 中间件）、Tailwind（仅此，无自定义 CSS/内联样式）、node-pg-migrate、Playwright e2e、LemonSqueezy。

**验证约定（项目约束适配）:** 本项目 `docs/constraints/testing-and-validation.md` 要求「不主动新增测试、happy-path MVP 验证」。故各任务验证用 `npx tsc --noEmit`、`npx eslint <改动文件>`、必要时 `npx next build`、以及 `pnpm dev` + curl/手验；不新建单元测试。复用并更新既有 e2e。

**关联 spec:** `docs/superpowers/specs/2026-06-18-marketing-and-billing-design.md`
**分支:** `feat_20260618_官网与订阅计划`（已含 P0/P1/OG/spec 依赖）

---

## 文件结构总览

**阶段1 移动/新建**
- `app/admin/{page.tsx,users/page.tsx,layout.tsx}` → `app/super-admin/`（git mv，平台后台）
- `app/(dashboard)/**` → `app/admin/(workspace)/**`（git mv，租户后台 + Sidebar 壳）
- `app/editor-next/**` → `app/admin/editor/**`（git mv，全屏编辑器）
- `lib/constants/routes.ts`（改：加 `/admin` 前缀、新增 Dashboard/SuperAdmin）
- `lib/proxy/auth-proxy.ts`（改：前缀门禁、白名单、重定向）
- `auth.ts`、`app/(auth)/login/page.tsx`（改：登录落点 `/`→`/admin`）
- `components/Sidebar.tsx`（改：链接走常量、首页指 `/admin`）
- e2e 路径更新

**阶段2 改/新建**
- `lib/plans.ts`（重构：单一数据源 + `PLAN_FEATURE_ROWS` + helpers）
- `components/billing/PlanComparison.tsx`（新建：共享套餐卡片+对比表）
- `app/pricing/page.tsx`（改：读 PlanComparison/PLAN_FEATURE_ROWS）
- `app/admin/(workspace)/billing/page.tsx`（改：landingPagesLimit + highlights）
- `components/billing/UpgradeDialog.tsx`（改：landingPagesLimit）
- `app/api/landing-pages/route.ts`（改：创建前强制 landingPagesLimit）
- `landing-editor/components/TemplateGalleryCard.tsx`（改：捕获 403 引导升级）
- `lib/constants/errors.ts`（确认 LIMIT_EXCEEDED 存在）
- `docs/billing-lemonsqueezy-setup.md`（新建：LS 接通清单）

**阶段3 改/新建**
- `app/page.tsx`（新建：营销首页 5 区块）
- `app/robots.ts`（改：平台域放开 `/`+`/pricing`、禁后台）
- `lib/proxy/auth-proxy.ts`（改：已登录访问 `/` → `/admin`）

---

## 阶段 1：路由分层与布局隔离

### Task 1: 平台后台迁到 /super-admin

**Files:**
- Move: `app/admin/` → `app/super-admin/`
- Modify: `app/super-admin/layout.tsx`（内部链接与文案）

- [ ] **Step 1: git mv 整目录**

```bash
cd /Users/lajiao/Work/zapbridge
git mv app/admin app/super-admin
```

- [ ] **Step 2: 核对并改写 super-admin layout 内部链接**

打开 `app/super-admin/layout.tsx`，把其中指向 `/admin`、`/admin/users` 的导航链接改为 `/super-admin`、`/super-admin/users`。逐个 grep 确认：

```bash
grep -nE "/admin" app/super-admin/layout.tsx app/super-admin/page.tsx app/super-admin/users/page.tsx
```

把命中的页面内链接（非 `/api/admin`）改为 `/super-admin*`。`/api/admin/*`（接口）保持不变。

- [ ] **Step 3: 验证 tsc**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: exit 0（auth-proxy 此时仍 gate `/admin`，但页面已不在该路径——下一任务统一改 proxy；tsc 不受影响）

- [ ] **Step 4: 暂不提交**（阶段1 为一次性搬家，末尾 Task 7 统一验证后提交；若偏好细粒度提交可在此 `git add -A && git commit -m "refactor(routing): 平台后台迁至 /super-admin"`）

---

### Task 2: 租户后台迁到 /admin/(workspace)

**Files:**
- Move: `app/(dashboard)/` → `app/admin/(workspace)/`

- [ ] **Step 1: 建目录并 git mv**

```bash
cd /Users/lajiao/Work/zapbridge
mkdir -p "app/admin"
git mv "app/(dashboard)" "app/admin/(workspace)"
```

结果：`app/admin/(workspace)/layout.tsx`（Sidebar 壳）、`app/admin/(workspace)/page.tsx`（→ `/admin`）、`app/admin/(workspace)/billing/page.tsx`（→ `/admin/billing`）等。`(workspace)` 路由组不产生路径段，故首页是 `/admin`。

- [ ] **Step 2: 确认 admin 段下无 layout.tsx**

```bash
ls app/admin/layout.tsx 2>/dev/null && echo "存在(需删,会把壳套到编辑器)" || echo "无 layout(正确)"
```

Expected: 「无 layout(正确)」——确保 `(workspace)` 外的 `editor/*`（Task 3）不被 Sidebar 壳包裹。

- [ ] **Step 3: 验证 tsc**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: exit 0

---

### Task 3: 编辑器迁到 /admin/editor（全屏）

**Files:**
- Move: `app/editor-next/` → `app/admin/editor/`

- [ ] **Step 1: git mv**

```bash
cd /Users/lajiao/Work/zapbridge
git mv app/editor-next "app/admin/editor"
```

结果：`app/admin/editor/page.tsx`（→ `/admin/editor`，模板库）、`app/admin/editor/[id]/page.tsx`（→ `/admin/editor/[id]`）、`app/admin/editor/[id]/preview/page.tsx`（→ `/admin/editor/[id]/preview`）。因在 `(workspace)` 组外，仅继承 root layout，全屏无 Sidebar。

- [ ] **Step 2: 验证 tsc**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: exit 0（路径助手仍指旧路径，但运行期才生效；Task 4 修常量）

---

### Task 4: 更新路由常量

**Files:**
- Modify: `lib/constants/routes.ts`

- [ ] **Step 1: 改写 Routes 与路径助手**

把 `lib/constants/routes.ts` 改为：

```ts
export enum Routes {
  Home = '/',                       // 营销首页（公开）
  Login = '/login',
  Register = '/register',
  Dashboard = '/admin',             // 租户后台首页
  Domains = '/admin/domains',
  Media = '/admin/media',
  Billing = '/admin/billing',
  Pricing = '/pricing',
  LandingPages = '/admin/landing-pages',
  SuperAdmin = '/super-admin',
}

export enum ApiRoutes {
  Domains = '/api/domains',
  Media = '/api/media',
  Templates = '/api/templates',
  Register = '/api/register',
  BillingCheckout = '/api/billing/checkout',
  BillingPortal = '/api/billing/portal',
  AdminTemplates = '/api/admin/templates',
  AdminInvite = '/api/admin/invite',
}

export const apiAdminTemplatePath = (id: string) => `/api/admin/templates/${id}`;
export const apiMediaPath = (id: string) => `/api/media/${id}`;

export const apiDomainPath = (id: string) => `/api/domains/${id}`;
export const apiDomainStatusPath = (id: string) => `/api/domains/${id}/status`;

export const landingEditorPath = (id: string) => `/admin/editor/${id}`;
export const landingPreviewPath = (id: string) => `/admin/editor/${id}/preview`;
export const apiLandingPagesPath = () => `/api/landing-pages`;
export const apiLandingPagePath = (id: string) => `/api/landing-pages/${id}`;
export const apiLandingPublishPath = (id: string) => `/api/landing-pages/${id}/publish`;
export const apiLandingUnpublishPath = (id: string) => `/api/landing-pages/${id}/unpublish`;
```

注：`ApiRoutes`/`/api/admin/*` 不变（接口不迁）。`新建落地页入口` 在 UI 里用 `/admin/editor`（见 Task 6 核对硬编码）。

- [ ] **Step 2: 验证 tsc**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: exit 0

---

### Task 5: 改造 auth-proxy（前缀门禁/白名单/重定向）

**Files:**
- Modify: `lib/proxy/auth-proxy.ts`

- [ ] **Step 1: 改写 PUBLIC_PATHS 与门禁前缀**

把 `lib/proxy/auth-proxy.ts` 改为（保留 `/p`、`/preview-next`、`/pricing`、`/robots.txt`、`/sitemap.xml` 公开；根 `/` 公开；super-admin 用 `/super-admin` 前缀，租户用 `/admin` 前缀需登录）：

```ts
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";
import { UserRole } from "@/lib/constants";

export const PUBLIC_PATHS = [
  "/login",
  "/register",
  "/pricing",
  "/preview-next",
  "/p",
  "/robots.txt",
  "/sitemap.xml",
  "/api/auth",
  "/api/register",
  "/api/templates",
];

type ProxyAuth = { user?: { role?: UserRole } } | null;

export function handleAuth(req: NextRequest & { auth?: ProxyAuth }) {
  const { pathname } = req.nextUrl;
  const isLoggedIn = !!req.auth;

  // 营销首页：公开；已登录则进后台
  if (pathname === "/") {
    return isLoggedIn ? NextResponse.redirect(new URL("/admin", req.url)) : null;
  }

  const isPublic = PUBLIC_PATHS.some((p) => pathname.startsWith(p));
  if (isPublic) return null;

  // 平台后台：需 SUPER_ADMIN
  if (pathname.startsWith("/super-admin")) {
    if (!isLoggedIn) return NextResponse.redirect(new URL("/login", req.url));
    if (req.auth?.user?.role !== UserRole.SUPER_ADMIN) {
      return NextResponse.redirect(new URL("/admin", req.url));
    }
    return null;
  }

  // API 保护（非公开）
  if (pathname.startsWith("/api")) {
    if (!isLoggedIn) {
      return new NextResponse(JSON.stringify({ error: "Unauthorized" }), {
        status: 401,
        headers: { "content-type": "application/json" },
      });
    }
    return null;
  }

  // 租户后台及其它页面：需登录
  if (!isLoggedIn) {
    return NextResponse.redirect(new URL("/login", req.url));
  }
  return null;
}
```

注：原 `/admin` 的 SUPER_ADMIN 门禁改为 `/super-admin`；`/admin/*` 现在只需登录（租户）。已登录访问 `/` → `/admin`（满足阶段3 登录跳后台，无需额外改动）。

- [ ] **Step 2: 验证 tsc + eslint**

Run: `npx tsc --noEmit -p tsconfig.json && npx eslint lib/proxy/auth-proxy.ts`
Expected: exit 0

---

### Task 6: 登录落点 + Sidebar + 残留硬编码

**Files:**
- Modify: `auth.ts`、`app/(auth)/login/page.tsx`、`components/Sidebar.tsx`，以及任何残留硬编码路径文件

- [ ] **Step 1: 登录后落点改为后台首页**

`app/(auth)/login/page.tsx` 三处 `Routes.Home`（callbackUrl 与两处 `router.push`）改为 `Routes.Dashboard`：

```bash
grep -n "Routes.Home" "app/(auth)/login/page.tsx"
```

把这三处 `Routes.Home` 替换为 `Routes.Dashboard`。`auth.ts:103` 的 `signIn: Routes.Login` 不变（那是登录页地址，非落点）。

- [ ] **Step 2: Sidebar 首页链接指向 /admin，其余走常量**

`components/Sidebar.tsx`：把 `{ label: "Dashboard", href: "/" }` 改为 `href: Routes.Dashboard`；其余硬编码（`/landing-pages`、`/domains`、`/statistics`、`/tasks`、`/reports`、`/notifications`、`/settings`、`/help`）改为对应 `Routes.*` 或加 `/admin` 前缀。确保 `import { Routes } from "@/lib/constants"` 存在。逐项核对：

```bash
grep -nE "href|/landing-pages|/domains|/statistics|/tasks|/reports|/notifications|/settings|/help|/admin" components/Sidebar.tsx
```

- [ ] **Step 3: 全仓扫描残留旧路径并改写**

```bash
grep -rnE "/editor-next|/preview-next|\"/\"|'/'|href=\"/(billing|domains|landing-pages|media|settings)" app components lib --include=*.tsx --include=*.ts | grep -vE "lib/constants/routes.ts|/api/|/preview-next/page|/p/"
```

逐条核对：编辑器入口（`landing-pages/page.tsx` 的 `/editor-next` Link、`TemplateGalleryCard`）等改为 `/admin/editor` 或 `landingEditorPath`。`/preview-next` 自身页面与 e2e 保留。注意 `app/admin/(workspace)/landing-pages/page.tsx` 里的 `Link href="/editor-next"` 要改 `/admin/editor`。

- [ ] **Step 4: 验证 tsc + eslint**

Run: `npx tsc --noEmit -p tsconfig.json && npx eslint app components lib`
Expected: exit 0（仅既有 `<img>` warning 允许）

---

### Task 7: 更新 e2e 路径 + 阶段1 整体验证 + 提交

**Files:**
- Modify: `e2e/editor-next-preview.spec.ts`、`e2e/preview-next.spec.ts`（路径）

- [ ] **Step 1: 更新 e2e 中的页面路径**

```bash
grep -rnE "/editor-next|goto\(" e2e --include=*.spec.ts
```

`editor-next-preview.spec.ts` 的 `page.goto("/editor-next")` → `"/admin/editor"`。`preview-next.spec.ts` 的 `/preview-next` 保留不变（demo 未迁）。`landing-pages-flow.spec.ts` 走自定义域名根路径，不受影响。

- [ ] **Step 2: 构建验证（确认路由树正确）**

Run: `npx next build`
Expected: exit 0；输出路由列表含 `/admin`、`/admin/billing`、`/admin/domains`、`/admin/editor/[id]`、`/super-admin`、`/super-admin/users`，不再有 `/billing`、`/editor-next`。

- [ ] **Step 3: dev 起服务 + 手验关键跳转**

```bash
(pnpm dev >/tmp/zapbridge-dev.log 2>&1 &)
for i in $(seq 1 40); do curl -sf -o /dev/null http://localhost:3001/login && break; sleep 1; done
# 未登录访问后台 → 跳登录（307）
curl -s -o /dev/null -w "%{http_code} %{redirect_url}\n" -H 'Host: localhost:3001' http://localhost:3001/admin
# 未登录访问 super-admin → 跳登录
curl -s -o /dev/null -w "%{http_code} %{redirect_url}\n" -H 'Host: localhost:3001' http://localhost:3001/super-admin
pkill -f "next dev -p 3001"; pkill -f "next-server"
```

Expected: 两者均 307 跳 `/login`。

- [ ] **Step 4: 跑既有 DB e2e（确认搬家未破坏发布渲染）**

```bash
RUN_DB_E2E=1 npx playwright test e2e/landing-pages-flow.spec.ts
```

Expected: 1 passed（多租户发布/取消发布渲染不受路由搬家影响）。

- [ ] **Step 5: 提交阶段1**

```bash
git add -A
git commit -m "refactor(routing): 路由分层——租户后台迁 /admin、编辑器迁 /admin/editor、平台后台迁 /super-admin"
```

---

## 阶段 2：订阅计划单一数据源 + 计费修复 + LS 清单

### Task 8: 重构 lib/plans.ts 为单一数据源

**Files:**
- Modify: `lib/plans.ts`

- [ ] **Step 1: 改写 PlanConfig 与 PLANS**

把 `lib/plans.ts` 改为（`sitesLimit`→`landingPagesLimit`；加营销 flags 与 highlights；加 `PLAN_FEATURE_ROWS`）：

```ts
export type PlanId = "free" | "starter" | "pro" | "agency";

export interface PlanConfig {
  id: PlanId;
  label: string;
  priceText: string;            // 仅展示
  color: string;
  highlight?: boolean;          // "最受欢迎"
  // 后端强制限额
  landingPagesLimit: number;
  domainsLimit: number;
  // 特性标记
  hasWatermark: boolean;
  allTemplates: boolean;
  advancedTracking: boolean;
  antiBan: boolean;
  aiTranslation: boolean;
  // 升级卡片要点（billing 用）
  highlights: string[];
}

export const PLANS: Record<PlanId, PlanConfig> = {
  free: {
    id: "free", label: "Free", priceText: "$0", color: "slate",
    landingPagesLimit: 1, domainsLimit: 0,
    hasWatermark: true, allTemplates: false, advancedTracking: false, antiBan: false, aiTranslation: false,
    highlights: ["1 张落地页", "可视化编辑器", "在线预览（发布需升级绑定域名）"],
  },
  starter: {
    id: "starter", label: "Starter", priceText: "$29/mo", color: "blue",
    landingPagesLimit: 3, domainsLimit: 1,
    hasWatermark: true, allTemplates: false, advancedTracking: false, antiBan: false, aiTranslation: false,
    highlights: ["3 张落地页 + 1 个自定义域名", "5 款王牌爆款模板", "1× Meta Pixel 追踪"],
  },
  pro: {
    id: "pro", label: "Pro", priceText: "$79/mo", color: "violet", highlight: true,
    landingPagesLimit: 20, domainsLimit: 5,
    hasWatermark: false, allTemplates: true, advancedTracking: true, antiBan: true, aiTranslation: false,
    highlights: ["20 张落地页 + 5 个域名", "全库 15+ 行业模板", "去除品牌水印", "全矩阵像素 + Meta CAPI", "反同质化风控引擎"],
  },
  agency: {
    id: "agency", label: "Agency", priceText: "$199/mo", color: "amber",
    landingPagesLimit: Infinity, domainsLimit: Infinity,
    hasWatermark: false, allTemplates: true, advancedTracking: true, antiBan: true, aiTranslation: true,
    highlights: ["无限落地页 + 无限域名", "含高转化行业模板", "AI 自动多语言翻译", "一切 Pro 功能"],
  },
};

export const PLAN_ORDER: PlanId[] = ["free", "starter", "pro", "agency"];

// 对比表行定义：valueFor 返回字符串（额度）或布尔（有无）
export interface PlanFeatureRow {
  label: string;
  valueFor: (plan: PlanConfig) => string | boolean;
}

const fmtLimit = (n: number, unit: string) => (n === Infinity ? "无限" : n === 0 ? "—" : `${n} ${unit}`);

export const PLAN_FEATURE_ROWS: PlanFeatureRow[] = [
  { label: "落地页数量", valueFor: (p) => fmtLimit(p.landingPagesLimit, "张") },
  { label: "自定义域名", valueFor: (p) => fmtLimit(p.domainsLimit, "个") },
  { label: "精美模板", valueFor: () => true },
  { label: "完整视觉编辑器", valueFor: () => true },
  { label: "基础数据追踪 (1× Meta Pixel)", valueFor: () => true },
  { label: "去除品牌水印", valueFor: (p) => !p.hasWatermark },
  { label: "全矩阵像素追踪 (TikTok / CAPI)", valueFor: (p) => p.advancedTracking },
  { label: "反同质化风控引擎", valueFor: (p) => p.antiBan },
  { label: "AI 多语言翻译", valueFor: (p) => p.aiTranslation },
];

export function getLandingPagesLimit(plan: PlanId): number {
  return PLANS[plan].landingPagesLimit;
}
export function hasWatermark(plan: PlanId): boolean {
  return PLANS[plan].hasWatermark;
}
export function canBindDomain(plan: PlanId): boolean {
  return PLANS[plan].domainsLimit > 0;
}
```

注：删除旧 `getSitesLimit`（已无调用方）、旧 `price`/`sitesLimit` 字段。

- [ ] **Step 2: tsc（会暴露所有引用旧字段处，下游任务逐一修）**

Run: `npx tsc --noEmit -p tsconfig.json 2>&1 | head -30`
Expected: 报错集中在 `pricing/page.tsx`、`billing/page.tsx`、`UpgradeDialog.tsx`（引用 `price`/`sitesLimit`/`FEATURE_VALUES` 已删）——这些在 Task 9-11 修复。先记录报错清单。

---

### Task 9: 共享 PlanComparison 组件

**Files:**
- Create: `components/billing/PlanComparison.tsx`

- [ ] **Step 1: 新建组件（套餐卡片 + 对比表，纯展示）**

```tsx
import Link from "next/link";
import { Check, X } from "lucide-react";
import { Routes } from "@/lib/constants";
import { PLANS, PLAN_ORDER, PLAN_FEATURE_ROWS } from "@/lib/plans";
import type { PlanId } from "@/lib/plans";

function FeatureValue({ value }: { value: string | boolean }) {
  if (typeof value === "boolean") {
    return value
      ? <Check className="w-4 h-4 text-emerald-500 mx-auto" />
      : <X className="w-4 h-4 text-slate-300 mx-auto" />;
  }
  return <span className="text-sm text-slate-700">{value}</span>;
}

/** 套餐卡片 + 功能对比表。ctaFor 决定每张卡片 CTA 的去向与文案。 */
export function PlanComparison({
  ctaFor,
  showTable = true,
}: {
  ctaFor?: (planId: PlanId) => { href: string; label: string };
  showTable?: boolean;
}) {
  const cta = ctaFor ?? ((planId: PlanId) =>
    planId === "free"
      ? { href: Routes.Register, label: "免费开始" }
      : { href: Routes.Billing, label: "立即升级" });

  return (
    <div>
      <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-12">
        {PLAN_ORDER.map((planId) => {
          const plan = PLANS[planId];
          const c = cta(planId);
          return (
            <div
              key={planId}
              className={`rounded-2xl border p-6 flex flex-col gap-4 ${
                plan.highlight
                  ? "border-violet-300 bg-violet-50 shadow-lg shadow-violet-100"
                  : "border-slate-200 bg-white"
              }`}
            >
              {plan.highlight && (
                <span className="self-start text-[10px] font-semibold uppercase tracking-wider px-2 py-0.5 rounded-full bg-violet-600 text-white">
                  最受欢迎
                </span>
              )}
              <div>
                <p className="text-xs uppercase tracking-widest text-slate-400 mb-1">{plan.label}</p>
                <p className="text-3xl font-bold text-slate-900">
                  {plan.priceText === "$0" ? "免费" : plan.priceText.split("/")[0]}
                  {plan.priceText !== "$0" && <span className="text-base font-normal text-slate-400">/月</span>}
                </p>
              </div>
              <Link
                href={c.href}
                className={`text-center text-sm font-medium py-2 rounded-xl transition-colors ${
                  plan.highlight
                    ? "bg-violet-600 text-white hover:bg-violet-700"
                    : "bg-slate-100 text-slate-700 hover:bg-slate-200"
                }`}
              >
                {c.label}
              </Link>
            </div>
          );
        })}
      </div>

      {showTable && (
        <div className="rounded-2xl border border-slate-200 overflow-hidden">
          <table className="w-full text-sm">
            <thead>
              <tr className="border-b border-slate-200 bg-slate-50">
                <th className="text-left px-6 py-4 text-slate-500 font-medium w-1/3">功能</th>
                {PLAN_ORDER.map((planId) => (
                  <th key={planId} className={`text-center px-4 py-4 font-semibold ${
                    PLANS[planId].highlight ? "text-violet-700" : "text-slate-700"
                  }`}>
                    {PLANS[planId].label}
                  </th>
                ))}
              </tr>
            </thead>
            <tbody>
              {PLAN_FEATURE_ROWS.map((row, i) => (
                <tr key={row.label} className={i % 2 === 0 ? "bg-white" : "bg-slate-50/50"}>
                  <td className="px-6 py-3 text-slate-600">{row.label}</td>
                  {PLAN_ORDER.map((planId) => (
                    <td key={planId} className="px-4 py-3 text-center">
                      <FeatureValue value={row.valueFor(PLANS[planId])} />
                    </td>
                  ))}
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: eslint 组件**

Run: `npx eslint components/billing/PlanComparison.tsx`
Expected: exit 0

---

### Task 10: 重构 /pricing 用 PlanComparison

**Files:**
- Modify: `app/pricing/page.tsx`

- [ ] **Step 1: 用共享组件替换本地 FEATURE_VALUES**

把 `app/pricing/page.tsx` 改为：

```tsx
import { PlanComparison } from "@/components/billing/PlanComparison";

export default function PricingPage() {
  return (
    <main className="min-h-screen bg-gradient-to-b from-slate-50 to-white py-20 px-4">
      <div className="max-w-5xl mx-auto">
        <div className="text-center mb-14">
          <h1 className="text-4xl font-bold text-slate-900 mb-3">选择适合你的套餐</h1>
          <p className="text-slate-500 text-lg">先用免费版起步，赚到第一桶金后再按需升级</p>
        </div>
        <PlanComparison />
      </div>
    </main>
  );
}
```

删除原 `FEATURE_VALUES`/`FEATURES`/`FeatureValue`/`PLAN_HIGHLIGHT` 等本地定义。

- [ ] **Step 2: tsc + eslint**

Run: `npx tsc --noEmit -p tsconfig.json && npx eslint app/pricing/page.tsx`
Expected: exit 0

---

### Task 11: 重构 /admin/billing + UpgradeDialog

**Files:**
- Modify: `app/admin/(workspace)/billing/page.tsx`、`components/billing/UpgradeDialog.tsx`

- [ ] **Step 1: billing 页改读 landingPagesLimit + highlights**

在 `app/admin/(workspace)/billing/page.tsx`：
- 删除本地 `UPGRADE_HIGHLIGHTS`，升级卡片要点改用 `PLANS[planId].highlights`。
- 当前套餐价格用 `currentPlan.priceText`（原 `currentPlan.price` 已删）。
- 「站点上限」块改为「落地页上限」，值用 `currentPlan.landingPagesLimit === Infinity ? "无限" : currentPlan.landingPagesLimit + " 张"`。

逐处定位：

```bash
grep -nE "UPGRADE_HIGHLIGHTS|\.price\b|sitesLimit|站点" "app/admin/(workspace)/billing/page.tsx"
```

把 `highlights = UPGRADE_HIGHLIGHTS[planId] ?? []` 改为 `highlights = PLANS[planId].highlights`；`currentPlan.price` → `currentPlan.priceText`；`plan.price` → `plan.priceText`。

- [ ] **Step 2: UpgradeDialog 改 landingPagesLimit**

`components/billing/UpgradeDialog.tsx`：

```bash
grep -nE "sitesLimit|站点" components/billing/UpgradeDialog.tsx
```

把 `current.sitesLimit`/`target.sitesLimit` → `current.landingPagesLimit`/`target.landingPagesLimit`，文案「站点」→「落地页」。

- [ ] **Step 3: tsc + eslint**

Run: `npx tsc --noEmit -p tsconfig.json && npx eslint "app/admin/(workspace)/billing/page.tsx" components/billing/UpgradeDialog.tsx`
Expected: exit 0（阶段2 至此 plans.ts 引用全部修齐，tsc 应回到 0）

---

### Task 12: 创建落地页强制 landingPagesLimit

**Files:**
- Modify: `app/api/landing-pages/route.ts`、`landing-editor/components/TemplateGalleryCard.tsx`
- Verify: `lib/constants/errors.ts`（LIMIT_EXCEEDED 存在）

- [ ] **Step 1: 确认错误码常量**

```bash
grep -niE "LIMIT_EXCEEDED|limit_exceeded" lib/constants/errors.ts lib/constants/*.ts
```

Expected: 存在 `LIMIT_EXCEEDED`（域名限额已用，见 `api/domains`）。若不存在则在 `ApiErrors` 加 `LIMIT_EXCEEDED = "limit_exceeded"`。

- [ ] **Step 2: 在 POST /api/landing-pages 创建前校验**

`app/api/landing-pages/route.ts` 的 POST，在 `getTemplate` 之前插入限额校验。改后 POST：

```ts
import { getUserPlan } from "@/lib/plans-db";
import { PLANS } from "@/lib/plans";
// ...existing imports（createLandingPage, listLandingPages, getTemplate, auth, ApiErrors）...

export async function POST(request: Request) {
  const session = await auth();
  if (!session?.user?.id) {
    return NextResponse.json({ error: ApiErrors.UNAUTHORIZED }, { status: 401 });
  }

  const plan = await getUserPlan(session.user.id);
  const limit = PLANS[plan].landingPagesLimit;
  if (limit !== Infinity) {
    const existing = await listLandingPages(session.user.id);
    if (existing.length >= limit) {
      return NextResponse.json({ error: ApiErrors.LIMIT_EXCEEDED }, { status: 403 });
    }
  }

  const { templateId } = await request.json();
  const template = getTemplate(templateId);
  const row = await createLandingPage(session.user.id, template.name, template.draft);
  return NextResponse.json(row, { status: 201 });
}
```

- [ ] **Step 3: 前端捕获 403 引导升级**

`landing-editor/components/TemplateGalleryCard.tsx`：创建请求若返回 403，提示「已达当前套餐落地页上限，请升级」并引导到 `/admin/billing`（或弹 UpgradeDialog）。定位现有 fetch：

```bash
grep -nE "api/landing-pages|res\.|status|ok" landing-editor/components/TemplateGalleryCard.tsx
```

在 `if (!res.ok)` 分支内，对 `res.status === 403` 用 `toast.error("已达当前套餐的落地页上限，请升级后再创建")` 并 `window.location.href = "/admin/billing"`（或调用既有升级弹窗）。其余错误保持原处理。

- [ ] **Step 4: tsc + eslint**

Run: `npx tsc --noEmit -p tsconfig.json && npx eslint app/api/landing-pages/route.ts landing-editor/components/TemplateGalleryCard.tsx`
Expected: exit 0

- [ ] **Step 5: dev 验证强制生效**

```bash
(pnpm dev >/tmp/zapbridge-dev.log 2>&1 &); for i in $(seq 1 40); do curl -sf -o /dev/null http://localhost:3001/login && break; sleep 1; done
CID=$(docker ps --filter "publish=5433" --format "{{.ID}}" | head -1)
# 造一个 free 用户已有 1 张落地页，再调用创建应 403
docker exec "$CID" psql -U postgres -d zapbridge -c "INSERT INTO users(id,email,plan) VALUES('lim-user','lim@test.local','free') ON CONFLICT(id) DO UPDATE SET plan='free'; INSERT INTO landing_pages(user_id,name,data) VALUES('lim-user','p1','{}'::jsonb);"
# 直接查计数验证逻辑（接口需登录态，难以 curl；以单元式 tsx 脚本或人工登录验证）
docker exec "$CID" psql -U postgres -d zapbridge -c "SELECT COUNT(*) FROM landing_pages WHERE user_id='lim-user';"
docker exec "$CID" psql -U postgres -d zapbridge -c "DELETE FROM landing_pages WHERE user_id='lim-user'; DELETE FROM users WHERE id='lim-user';"
pkill -f "next dev -p 3001"; pkill -f "next-server"
```

Expected: 计数为 1，确认 fixture 正确（接口 403 在登录态人工复验，或写一次性 tsx 调用 POST 逻辑后删脚本）。

---

### Task 13: LemonSqueezy 接通清单文档

**Files:**
- Create: `docs/billing-lemonsqueezy-setup.md`

- [ ] **Step 1: 写配置清单**

新建 `docs/billing-lemonsqueezy-setup.md`，内容含：所需 env 变量表（`LEMONSQUEEZY_API_KEY`、`LEMONSQUEEZY_STORE_ID`、`LEMONSQUEEZY_WEBHOOK_SECRET`、`LEMONSQUEEZY_VARIANT_STARTER/PRO/AGENCY`）与各自用途与取值位置；LS 后台步骤（建 Store → 建 starter/pro/agency 三个 variant 取 variant_id → Settings/Webhooks 新建 webhook URL=`https://<域名>/api/webhooks/lemonsqueezy`、勾选 `subscription_created/updated/cancelled/expired`、复制 signing secret）；在 Vercel Project Settings → Environment Variables 配置；本地 `.env.local` 对应键。注明代码侧 `lib/lemonsqueezy.ts` 已就绪。

- [ ] **Step 2: 提交阶段2**

```bash
git add -A
git commit -m "feat(billing): 订阅计划单一数据源 + landingPagesLimit 强制 + LS 接通清单"
```

---

## 阶段 3：营销首页

### Task 14: 修正平台域 robots

**Files:**
- Modify: `app/robots.ts`

- [ ] **Step 1: 平台域放开 / 与 /pricing、禁后台**

把 `app/robots.ts` 平台域分支改为允许收录营销面、禁后台：

```ts
import type { MetadataRoute } from "next";
import { headers } from "next/headers";
import { hostnameOf, isCustomDomain } from "@/lib/host";

export default async function robots(): Promise<MetadataRoute.Robots> {
  const hostname = hostnameOf((await headers()).get("host"));

  // 租户自有域名：托管单张已发布落地页，允许收录 + sitemap。
  if (isCustomDomain(hostname)) {
    return {
      rules: { userAgent: "*", allow: "/" },
      sitemap: `https://${hostname}/sitemap.xml`,
    };
  }

  // 平台主域：放开营销面，禁后台与接口。
  return {
    rules: { userAgent: "*", allow: "/", disallow: ["/admin", "/super-admin", "/api"] },
  };
}
```

- [ ] **Step 2: tsc + eslint**

Run: `npx tsc --noEmit -p tsconfig.json && npx eslint app/robots.ts`
Expected: exit 0

---

### Task 15: 营销首页 app/page.tsx

**Files:**
- Create: `app/page.tsx`

- [ ] **Step 1: 新建营销首页（5 区块，Server Component，纯 Tailwind）**

新建 `app/page.tsx`，复用 `PlanComparison`：

```tsx
import type { Metadata } from "next";
import Link from "next/link";
import { LayoutTemplate, Pencil, Globe } from "lucide-react";
import { Routes } from "@/lib/constants";
import { PlanComparison } from "@/components/billing/PlanComparison";

export const metadata: Metadata = {
  title: "Zap Bridge — 几分钟做出并发布投放级落地页",
  description: "无需开发，基于行业模板做出并发布一张投放级落地页到你自己的域名，专为海外获客打造。",
  openGraph: {
    type: "website",
    title: "Zap Bridge — 几分钟做出并发布投放级落地页",
    description: "无需开发，基于行业模板做出并发布投放级落地页到你自己的域名。",
  },
};

const STEPS = [
  { icon: LayoutTemplate, title: "选模板", desc: "从行业模板库挑一套，开箱即用。" },
  { icon: Pencil, title: "改内容", desc: "可视化编辑文案与图片，自动保存、实时预览。" },
  { icon: Globe, title: "发布到自有域名", desc: "绑定你的品牌域名，一键发布，利于投放与 SEO。" },
];

export default function MarketingHome() {
  return (
    <main className="min-h-screen bg-white text-slate-900">
      {/* 1. 顶部导航 */}
      <header className="sticky top-0 z-20 border-b border-slate-100 bg-white/80 backdrop-blur">
        <div className="max-w-6xl mx-auto flex items-center justify-between px-6 py-4">
          <span className="text-lg font-bold tracking-tight">Zap Bridge</span>
          <nav className="flex items-center gap-4 text-sm">
            <Link href={Routes.Pricing} className="text-slate-600 hover:text-slate-900">套餐定价</Link>
            <Link href={Routes.Login} className="text-slate-600 hover:text-slate-900">登录</Link>
            <Link href={Routes.Register} className="rounded-lg bg-slate-900 px-4 py-2 text-white hover:bg-slate-700">免费开始</Link>
          </nav>
        </div>
      </header>

      {/* 2. Hero */}
      <section className="max-w-4xl mx-auto px-6 pt-20 pb-16 text-center">
        <h1 className="text-4xl md:text-5xl font-bold tracking-tight leading-tight">
          无需开发，几分钟做出并发布<br className="hidden md:block" />投放级落地页到你自己的域名
        </h1>
        <p className="mt-5 text-lg text-slate-500">
          为做海外获客的个人创业者与小团队打造：选模板 → 改内容 → 发布，页面跑在你的品牌域名上。
        </p>
        <div className="mt-8 flex items-center justify-center gap-3">
          <Link href={Routes.Register} className="rounded-xl bg-slate-900 px-6 py-3 text-sm font-medium text-white hover:bg-slate-700">免费开始</Link>
          <Link href={Routes.Pricing} className="rounded-xl border border-slate-200 px-6 py-3 text-sm font-medium text-slate-700 hover:bg-slate-50">查看套餐</Link>
        </div>
      </section>

      {/* 3. 三步流程 */}
      <section className="max-w-5xl mx-auto px-6 py-16">
        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
          {STEPS.map(({ icon: Icon, title, desc }) => (
            <div key={title} className="rounded-2xl border border-slate-200 p-6">
              <Icon className="w-6 h-6 text-slate-900" />
              <p className="mt-3 font-semibold">{title}</p>
              <p className="mt-1 text-sm text-slate-500">{desc}</p>
            </div>
          ))}
        </div>
      </section>

      {/* 4. 套餐区（复用单一数据源） */}
      <section className="max-w-5xl mx-auto px-6 py-16">
        <div className="text-center mb-12">
          <h2 className="text-3xl font-bold">简单透明的定价</h2>
          <p className="mt-2 text-slate-500">先用免费版起步，按需升级。</p>
        </div>
        <PlanComparison showTable={false} />
        <div className="text-center mt-6">
          <Link href={Routes.Pricing} className="text-sm text-violet-600 hover:underline">查看完整功能对比 →</Link>
        </div>
      </section>

      {/* 5. 页脚 */}
      <footer className="border-t border-slate-100">
        <div className="max-w-6xl mx-auto px-6 py-10 flex flex-col md:flex-row items-center justify-between gap-4 text-sm text-slate-500">
          <span>© {new Date().getFullYear()} Zap Bridge</span>
          <nav className="flex items-center gap-4">
            <Link href={Routes.Pricing} className="hover:text-slate-900">套餐定价</Link>
            <Link href={Routes.Login} className="hover:text-slate-900">登录</Link>
            <Link href={Routes.Register} className="hover:text-slate-900">免费开始</Link>
          </nav>
        </div>
      </footer>
    </main>
  );
}
```

注：`PlanComparison` 默认 `ctaFor` 使 free→注册、付费→`/admin/billing`；首页未登录用户点付费卡会被 auth 引导登录，符合预期。

- [ ] **Step 2: tsc + eslint**

Run: `npx tsc --noEmit -p tsconfig.json && npx eslint app/page.tsx`
Expected: exit 0

---

### Task 16: 阶段3 验证 + 提交

- [ ] **Step 1: 构建**

Run: `npx next build`
Expected: exit 0；`/` 出现在路由列表（`ƒ` 动态，因 robots/auth 走 host）。

- [ ] **Step 2: dev 起服务，验证首页与登录态跳转**

```bash
(pnpm dev >/tmp/zapbridge-dev.log 2>&1 &); for i in $(seq 1 40); do curl -sf -o /dev/null http://localhost:3001/login && break; sleep 1; done
# 未登录 / 渲染营销首页（含品牌与 CTA）
curl -s -H 'Host: localhost:3001' http://localhost:3001/ | grep -oE "Zap Bridge|免费开始|简单透明的定价" | sort -u
# 平台域 robots 放开 / 禁后台
curl -s -H 'Host: localhost:3001' http://localhost:3001/robots.txt
# 租户域 robots 不变（allow + sitemap）
curl -s -H 'Host: verify.test' http://localhost:3001/robots.txt
pkill -f "next dev -p 3001"; pkill -f "next-server"
```

Expected: 首页含「Zap Bridge / 免费开始 / 简单透明的定价」；平台域 robots 含 `Disallow: /admin`、`/super-admin`、`/api` 且 `Allow: /`；租户域仍 `Allow: /` + Sitemap。（已登录 `/`→`/admin` 跳转在登录态人工复验。）

- [ ] **Step 3: 提交阶段3**

```bash
git add -A
git commit -m "feat(marketing): 新增公开营销首页 + 修正平台域 robots 放开营销面"
```

---

## Self-Review 检查（写计划后自查，已通过）

- **Spec 覆盖**：阶段1 路由分层（Task1-7）、阶段2 数据源+强制+文案+LS 清单（Task8-13）、阶段3 首页+robots（Task14-16）——spec 各节均有对应任务。
- **占位符**：无 TBD/TODO；非平凡逻辑（plans.ts、PlanComparison、auth-proxy、强制校验、robots、首页）均给完整代码。
- **类型一致**：`landingPagesLimit`/`priceText`/`highlights`/`PLAN_FEATURE_ROWS`/`PlanComparison` 命名在 Task8 定义后，Task9-15 引用一致；`Routes.Dashboard`/`Routes.SuperAdmin` 在 Task4 定义、Task5-6 使用一致。
- **依赖顺序**：Task8（数据源）先于 Task9-12（消费方）；Task1-3（搬家）先于 Task6/11（按新路径引用）。
