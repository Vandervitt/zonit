# Phase 1：草稿分享预览链接 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让登录用户为任意落地页（草稿或已发布）生成一个平台域下、带签名 token 的可分享预览链接，未登录访客也能打开查看真实渲染效果——从而把「见到线上效果」的 aha moment 提前到绑定自有域名之前。

**Architecture:** 预览链接形如 `https://zapbridge.tech/preview/<token>`。token = `${pageId}.${exp}.${sigBase64url}`，其中 `sig = HMAC-SHA256(AUTH_SECRET, "${pageId}.${exp}.${preview_secret}")`。`preview_secret` 是每页一枚、存在 `landing_pages.preview_secret` 的随机串：**轮换它即撤销所有旧链接**；`exp` 提供 7 天默认过期；`AUTH_SECRET` 防止伪造。预览路由无 owner 鉴权（凭有效签名放行），但强制 `noindex`（meta + `X-Robots-Tag` 响应头）、强制水印、**不注入任何像素/追踪**——因此预览页不是「托管发布」，不违背「落地页只能发布到自有域名」硬约束。

**Tech Stack:** Next.js App Router（本仓魔改版，改动前读 `node_modules/next/dist/docs/`）、node `crypto`、node-pg-migrate、Postgres（`lib/db` pool）、Tailwind-only、vitest（纯逻辑 TDD）、Playwright（e2e）。

**风险定级：** 中高（新增公开访问面 + 签名 token + 触碰中间件白名单 + DB 迁移）。故 token 纯逻辑走严格 TDD，路由/面板走「实现 + e2e 同批交付」，DB 迁移遵循 `docs/dev-database-migration-workflow.md`。

---

## 关键设计决策（已与用户确认，见主路线图）

| 点 | 结论 |
|---|---|
| 链接落点 | 平台域 `zapbridge.tech/preview/<token>`（非自有域、非 `/p` 托管） |
| 撤销 | 轮换 `preview_secret`（旧链接立即失效） |
| 过期 | 默认 7 天（`exp` 编入 token 并签名，客户端不可篡改） |
| 收录 | 强制 `noindex`：`generateMetadata` robots + `next.config` 的 `X-Robots-Tag` 头 |
| 水印 | 强制渲染 `<Watermark/>`，与 owner 套餐无关 |
| 追踪 | 预览页**不挂** `TrackingProvider`，不注入任何像素、不发 beacon |
| 草稿也可预览 | 预览路由不按 `status` 过滤（草稿是主要用例） |

---

## File Structure

**新建：**
- `migrations/018_add_preview_secret.js` — 给 `landing_pages` 加 `preview_secret TEXT`（可空）
- `lib/preview/token.ts` — 纯函数：签发 / 校验 / 解码 token
- `lib/preview/token.test.ts` — token 单测（TDD 主战场）
- `app/api/landing-pages/[id]/preview-link/route.ts` — POST：确保/轮换 secret、签发 token、返回 url
- `app/preview/[token]/page.tsx` — 公开预览渲染页（强制水印 + noindex + 无追踪）
- `landing-editor/components/SharePreviewPanel.tsx` — 编辑器「分享预览」面板（生成/复制/重置链接）
- `e2e/preview-share.spec.ts` — 端到端：建页 → 生成链接 → 无会话上下文访问 → 断言内容/水印/noindex；重置后旧链接 404

**修改：**
- `lib/landing-pages/store.ts` — `LandingPageRow` 加 `preview_secret`；新增 `getPageForPreview` / `ensurePreviewSecret` / `rotatePreviewSecret`
- `lib/constants/routes.ts` — 加 `previewSharePath(token)`、`apiLandingPreviewLinkPath(id)`
- `lib/proxy/auth-proxy.ts` — `PUBLIC_PATHS` 显式加 `/preview`
- `next.config.ts` — `headers()` 给 `/preview/:path*` 加 `X-Robots-Tag: noindex, nofollow`
- `landing-editor/components/EditorToolbar.tsx` — 「预览」旁加「分享预览」按钮 + 挂载面板

---

## Task 1：DB 迁移——landing_pages.preview_secret

**Files:**
- Create: `migrations/018_add_preview_secret.js`

- [ ] **Step 1: 写迁移（幂等，跟随 017 的写法）**

`migrations/018_add_preview_secret.js`：
```js
/** @type {import('node-pg-migrate').MigrationBuilder} */
exports.up = (pgm) => {
  pgm.sql(`
    ALTER TABLE landing_pages
      ADD COLUMN IF NOT EXISTS preview_secret TEXT;
  `);
};

exports.down = (pgm) => {
  pgm.sql(`
    ALTER TABLE landing_pages
      DROP COLUMN IF EXISTS preview_secret;
  `);
};
```

- [ ] **Step 2: 对本地 dev 库跑迁移**

Run: `pnpm migrate:up`
Expected: 输出含 `Migrating files: 018_add_preview_secret` 且无报错；重复跑一次应为 no-op（幂等）。

- [ ] **Step 3: 校验列已存在**

Run: `node -e "const p=require('./test-db.js')" 2>/dev/null || psql "$DATABASE_URL_UNPOOLED" -c "\d landing_pages" | grep preview_secret`
（若无 psql，用项目 `test-db.js` 或 `pnpm migrate:status` 确认 018 已 applied。）
Expected: 能看到 `preview_secret | text`。

- [ ] **Step 4: Commit**

```bash
git add migrations/018_add_preview_secret.js
git commit -m "feat: landing_pages 增加 preview_secret 列（草稿分享预览撤销用）"
```

---

## Task 2：token 纯逻辑（TDD）

**Files:**
- Create: `lib/preview/token.ts`
- Test: `lib/preview/token.test.ts`

- [ ] **Step 1: 写失败测试**

`lib/preview/token.test.ts`：
```ts
import { describe, it, expect } from "vitest";
import { signPreviewToken, verifyPreviewToken, decodePageId } from "./token";

const authSecret = "test-auth-secret";
const previewSecret = "page-secret-abc";
const pageId = "a319f808-7d6f-430d-b5d2-a0736a30146d";
const now = 1_000_000_000_000;

function makeToken(overrides: Partial<{ expMs: number; previewSecret: string }> = {}) {
  return signPreviewToken({
    pageId,
    expMs: overrides.expMs ?? now + 7 * 24 * 3600 * 1000,
    previewSecret: overrides.previewSecret ?? previewSecret,
    authSecret,
  });
}

describe("preview token", () => {
  it("签发→校验往返成功，返回 pageId", () => {
    const t = makeToken();
    expect(verifyPreviewToken({ token: t, previewSecret, authSecret, nowMs: now })).toEqual({ pageId });
  });

  it("过期 token 校验失败", () => {
    const t = makeToken({ expMs: now - 1 });
    expect(verifyPreviewToken({ token: t, previewSecret, authSecret, nowMs: now })).toBeNull();
  });

  it("preview_secret 轮换后旧 token 失效（撤销语义）", () => {
    const t = makeToken({ previewSecret: "old-secret" });
    expect(verifyPreviewToken({ token: t, previewSecret: "new-secret", authSecret, nowMs: now })).toBeNull();
  });

  it("篡改签名（错误 AUTH_SECRET）校验失败", () => {
    const t = makeToken();
    expect(verifyPreviewToken({ token: t, previewSecret, authSecret: "wrong", nowMs: now })).toBeNull();
  });

  it("篡改 pageId 后签名不匹配", () => {
    const t = makeToken();
    const tampered = "b0000000-0000-0000-0000-000000000000" + t.slice(t.indexOf("."));
    expect(verifyPreviewToken({ token: tampered, previewSecret, authSecret, nowMs: now })).toBeNull();
  });

  it("decodePageId 不校验签名，仅解出 pageId 供查库", () => {
    const t = makeToken();
    expect(decodePageId(t)).toBe(pageId);
  });

  it("格式非法（缺段）返回 null", () => {
    expect(verifyPreviewToken({ token: "garbage", previewSecret, authSecret, nowMs: now })).toBeNull();
    expect(decodePageId("garbage")).toBeNull();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm vitest run lib/preview/token.test.ts`
Expected: FAIL（`Cannot find module './token'`）。

- [ ] **Step 3: 实现 token 模块**

`lib/preview/token.ts`：
```ts
// 草稿分享预览的签名 token。
// token = `${pageId}.${exp}.${sig}`，sig = base64url(HMAC-SHA256(AUTH_SECRET, `${pageId}.${exp}.${previewSecret}`))。
// AUTH_SECRET 防伪造；per-page previewSecret 提供撤销（轮换即失效）；exp 提供过期。
import { createHmac, timingSafeEqual } from "node:crypto";

function b64url(buf: Buffer): string {
  return buf.toString("base64").replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
}

function sign(pageId: string, exp: number, previewSecret: string, authSecret: string): string {
  const mac = createHmac("sha256", authSecret).update(`${pageId}.${exp}.${previewSecret}`).digest();
  return b64url(mac);
}

export function signPreviewToken(args: {
  pageId: string;
  expMs: number;
  previewSecret: string;
  authSecret: string;
}): string {
  const { pageId, expMs, previewSecret, authSecret } = args;
  return `${pageId}.${expMs}.${sign(pageId, expMs, previewSecret, authSecret)}`;
}

/** 仅结构解析出 pageId（不校验签名），供预览路由先查库拿 previewSecret。非法返回 null。 */
export function decodePageId(token: string): string | null {
  const parts = token.split(".");
  if (parts.length !== 3 || !parts[0]) return null;
  return parts[0];
}

/** 校验签名 + 过期。通过返回 { pageId }，否则 null。 */
export function verifyPreviewToken(args: {
  token: string;
  previewSecret: string;
  authSecret: string;
  nowMs: number;
}): { pageId: string } | null {
  const { token, previewSecret, authSecret, nowMs } = args;
  const parts = token.split(".");
  if (parts.length !== 3) return null;
  const [pageId, expStr, sig] = parts;
  const exp = Number(expStr);
  if (!pageId || !Number.isFinite(exp) || exp <= nowMs) return null;

  const expected = sign(pageId, exp, previewSecret, authSecret);
  const a = Buffer.from(sig);
  const b = Buffer.from(expected);
  if (a.length !== b.length || !timingSafeEqual(a, b)) return null;
  return { pageId };
}
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm vitest run lib/preview/token.test.ts`
Expected: PASS（7 用例全绿）。

- [ ] **Step 5: Commit**

```bash
git add lib/preview/token.ts lib/preview/token.test.ts
git commit -m "feat: 草稿分享预览的签名 token 模块（签发/校验/撤销/过期）"
```

---

## Task 3：store 层——preview_secret 读写

**Files:**
- Modify: `lib/landing-pages/store.ts`

> 说明：store 函数直连 `lib/db` pool，本仓无 store 单测传统（靠 e2e 覆盖）。保持函数薄，行为在 Task 7 的 e2e 验证。

- [ ] **Step 1: `LandingPageRow` 增加字段**

`lib/landing-pages/store.ts`，在 `interface LandingPageRow` 内 `updated_at: string;` 后加：
```ts
  preview_secret: string | null;
```

- [ ] **Step 2: 新增预览专用查询与 secret 读写函数**

在文件末尾追加：
```ts
/** 预览渲染用：按 id 取页面（不按 owner、不按 status 过滤——草稿也可预览）。 */
export async function getPageForPreview(id: string): Promise<LandingPageRow | null> {
  const result = await pool.query(`SELECT * FROM landing_pages WHERE id = $1`, [id]);
  return result.rows[0] ?? null;
}

/** 确保该页有 preview_secret：无则惰性生成并持久化，返回最终值。按 owner 隔离。 */
export async function ensurePreviewSecret(id: string, userId: string): Promise<string | null> {
  const secret = randomBytes(16).toString("hex");
  const result = await pool.query(
    `UPDATE landing_pages
       SET preview_secret = COALESCE(preview_secret, $1)
     WHERE id = $2 AND user_id = $3
     RETURNING preview_secret`,
    [secret, id, userId],
  );
  return result.rows[0]?.preview_secret ?? null;
}

/** 轮换 preview_secret（撤销所有旧链接）。按 owner 隔离，返回新值。 */
export async function rotatePreviewSecret(id: string, userId: string): Promise<string | null> {
  const secret = randomBytes(16).toString("hex");
  const result = await pool.query(
    `UPDATE landing_pages SET preview_secret = $1 WHERE id = $2 AND user_id = $3 RETURNING preview_secret`,
    [secret, id, userId],
  );
  return result.rows[0]?.preview_secret ?? null;
}
```

- [ ] **Step 3: 顶部引入 randomBytes**

`lib/landing-pages/store.ts` 第 1 行 `import pool from "@/lib/db";` 上方加：
```ts
import { randomBytes } from "node:crypto";
```

- [ ] **Step 4: 类型检查**

Run: `pnpm tsc --noEmit`
Expected: 无错误。

- [ ] **Step 5: Commit**

```bash
git add lib/landing-pages/store.ts
git commit -m "feat: store 增加 preview_secret 读写与预览取页函数"
```

---

## Task 4：路由常量

**Files:**
- Modify: `lib/constants/routes.ts`

- [ ] **Step 1: 加路径 helper**

`lib/constants/routes.ts` 末尾追加：
```ts
export const previewSharePath = (token: string) => `/preview/${token}`;
export const apiLandingPreviewLinkPath = (id: string) => `/api/landing-pages/${id}/preview-link`;
```

- [ ] **Step 2: 类型检查**

Run: `pnpm tsc --noEmit`
Expected: 无错误。

- [ ] **Step 3: Commit**

```bash
git add lib/constants/routes.ts
git commit -m "feat: 预览分享路由常量"
```

---

## Task 5：生成预览链接的 API

**Files:**
- Create: `app/api/landing-pages/[id]/preview-link/route.ts`

- [ ] **Step 1: 写路由（模仿 publish/route.ts 的鉴权与错误码）**

`app/api/landing-pages/[id]/preview-link/route.ts`：
```ts
import type { NextRequest } from "next/server";
import { NextResponse } from "next/server";
import { auth } from "@/auth";
import { ApiErrors } from "@/lib/constants";
import { getLandingPage, ensurePreviewSecret, rotatePreviewSecret } from "@/lib/landing-pages/store";
import { signPreviewToken } from "@/lib/preview/token";
import { previewSharePath } from "@/lib/constants/routes";

const PREVIEW_TTL_MS = 7 * 24 * 3600 * 1000;

// 自包含拼接绝对 URL（剥 base 尾斜杠，避免 //preview）。不依赖 PR #30 的 lib/host.appUrl，
// 使本分支与 #30 合并顺序解耦；#30 合入后可选改用 appUrl 收敛。
const absoluteUrl = (path: string) => `${(process.env.NEXT_PUBLIC_APP_URL ?? "").replace(/\/+$/, "")}${path}`;

export async function POST(request: NextRequest, ctx: RouteContext<"/api/landing-pages/[id]/preview-link">) {
  const session = await auth();
  if (!session?.user?.id) return NextResponse.json({ error: ApiErrors.UNAUTHORIZED }, { status: 401 });

  const { id } = await ctx.params;
  const page = await getLandingPage(id, session.user.id);
  if (!page) return NextResponse.json({ error: ApiErrors.NOT_FOUND }, { status: 404 });

  let rotate = false;
  try { rotate = (await request.json())?.rotate === true; } catch { /* 空 body：默认不轮换 */ }

  const secret = rotate
    ? await rotatePreviewSecret(id, session.user.id)
    : await ensurePreviewSecret(id, session.user.id);
  if (!secret) return NextResponse.json({ error: ApiErrors.NOT_FOUND }, { status: 404 });

  const expMs = Date.now() + PREVIEW_TTL_MS;
  const token = signPreviewToken({ pageId: id, expMs, previewSecret: secret, authSecret: process.env.AUTH_SECRET ?? "" });

  return NextResponse.json({ url: absoluteUrl(previewSharePath(token)), expiresAt: new Date(expMs).toISOString() });
}
```

- [ ] **Step 2: 确认 `ApiErrors.NOT_FOUND` / `UNAUTHORIZED` 存在**

Run: `grep -nE "NOT_FOUND|UNAUTHORIZED" lib/constants/errors.ts`
Expected: 两者都在。（若命名不同，改用实际枚举值——与 publish/route.ts 一致即可。）

- [ ] **Step 3: 类型检查**

Run: `pnpm tsc --noEmit`
Expected: 无错误。

- [ ] **Step 4: Commit**

```bash
git add "app/api/landing-pages/[id]/preview-link/route.ts"
git commit -m "feat: 生成/轮换草稿分享预览链接的 API"
```

---

## Task 6：公开预览渲染页 + 中间件白名单 + noindex 头

**Files:**
- Create: `app/preview/[token]/page.tsx`
- Modify: `lib/proxy/auth-proxy.ts`
- Modify: `next.config.ts`

- [ ] **Step 1: 中间件放行 `/preview`**

`lib/proxy/auth-proxy.ts` 的 `PUBLIC_PATHS` 数组，在 `"/p",` 下一行加：
```ts
  "/preview", // 草稿分享预览：凭签名 token 公开访问，无需登录
```
（注：现有 `startsWith("/p")` 已偶然覆盖 `/preview`，此处显式声明以免日后 `/p` 收紧时误伤。）

- [ ] **Step 2: `X-Robots-Tag` 响应头**

`next.config.ts` 的 `nextConfig` 对象内，`images` 块后加：
```ts
  async headers() {
    return [
      { source: "/preview/:path*", headers: [{ key: "X-Robots-Tag", value: "noindex, nofollow" }] },
    ];
  },
```

- [ ] **Step 3: 写预览页**

`app/preview/[token]/page.tsx`：
```tsx
import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { LandingPage } from "@/landing-renderer/LandingPage";
import { Watermark } from "@/landing-renderer/Watermark";
import { getPageForPreview } from "@/lib/landing-pages/store";
import { getUserPlan } from "@/lib/plans-db";
import { hasAntiBan } from "@/lib/plans";
import { deriveVariant, IDENTITY_VARIANT } from "@/landing-renderer/variant";
import { decodePageId, verifyPreviewToken } from "@/lib/preview/token";
import { resolvePageMeta } from "@/lib/seo/resolve";

// 校验 token → 取页。任何失败一律 notFound（不泄露页面是否存在）。
async function loadValidPreview(token: string) {
  const pageId = decodePageId(token);
  if (!pageId) return null;
  const page = await getPageForPreview(pageId);
  if (!page || !page.preview_secret) return null;
  const ok = verifyPreviewToken({
    token,
    previewSecret: page.preview_secret,
    authSecret: process.env.AUTH_SECRET ?? "",
    nowMs: Date.now(),
  });
  return ok ? page : null;
}

// 预览页永远 noindex（叠加 next.config 的 X-Robots-Tag 头）；不派生 OG/canonical，避免被分享抓取当正式页。
export async function generateMetadata({ params }: { params: Promise<{ token: string }> }): Promise<Metadata> {
  const { token } = await params;
  const page = await loadValidPreview(token);
  const title = page ? `预览 · ${resolvePageMeta(page.data).title}` : "预览";
  return { title, robots: { index: false, follow: false } };
}

export default async function PreviewByToken({ params }: { params: Promise<{ token: string }> }) {
  const { token } = await params;
  const page = await loadValidPreview(token);
  if (!page) notFound();

  // 预览忠实反映发布后外观：按 owner 套餐派生反同质化变体（与公开页一致）。
  // 但：强制水印（与套餐无关）、不挂 TrackingProvider（预览不产生投放数据）。
  const plan = await getUserPlan(page.user_id);
  const variant = hasAntiBan(plan) ? deriveVariant(page.data.variantSeed ?? page.id) : IDENTITY_VARIANT;

  return (
    <>
      <LandingPage page={page.data} pageId={page.id} variant={variant} />
      <Watermark />
    </>
  );
}
```

- [ ] **Step 4: 类型检查 + 构建**

Run: `pnpm tsc --noEmit && pnpm build 2>&1 | tail -15`
Expected: tsc 无错；build 成功，输出的路由清单含 `/preview/[token]`。

- [ ] **Step 5: 手动冒烟（本地 dev）**

在本地 dev（`pnpm dev`）：
1. Dev Login 建页拿到 `<id>`。
2. `curl -s -X POST localhost:3000/api/landing-pages/<id>/preview-link -H 'cookie: <会话cookie>' -d '{}'` 拿 `url`。
3. 用**无 cookie** 的 `curl -si <url的path>` 访问：
Run（示意）: `curl -si "http://localhost:3000/preview/<token>" | grep -iE "HTTP/|x-robots-tag|Made with|noindex"`
Expected: `200`；响应头含 `x-robots-tag: noindex, nofollow`；HTML 含水印 `Made with` 与 `<meta name="robots" ... noindex>`；篡改 token 末位字符 → `404`。

- [ ] **Step 6: Commit**

```bash
git add "app/preview/[token]/page.tsx" lib/proxy/auth-proxy.ts next.config.ts
git commit -m "feat: 公开草稿预览渲染页（强制水印+noindex，无追踪）与中间件放行"
```

---

## Task 7：编辑器「分享预览」面板与按钮

**Files:**
- Create: `landing-editor/components/SharePreviewPanel.tsx`
- Modify: `landing-editor/components/EditorToolbar.tsx`

- [ ] **Step 1: 写面板（模仿 AntiBanPanel 的弹层结构，Tailwind-only）**

`landing-editor/components/SharePreviewPanel.tsx`：
```tsx
"use client";
import { useState } from "react";
import { useMeta } from "../MetaContext";
import { apiLandingPreviewLinkPath } from "@/lib/constants/routes";

/** 分享预览面板：生成/复制可分享链接；重置使旧链接失效。链接带水印、noindex、7 天过期、不投放数据。 */
export function SharePreviewPanel({ onClose }: { onClose: () => void }) {
  const { pageId } = useMeta();
  const [url, setUrl] = useState<string>("");
  const [expiresAt, setExpiresAt] = useState<string>("");
  const [busy, setBusy] = useState(false);
  const [copied, setCopied] = useState(false);
  const [error, setError] = useState("");

  async function request(rotate: boolean) {
    setBusy(true); setError(""); setCopied(false);
    try {
      const res = await fetch(apiLandingPreviewLinkPath(pageId), {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ rotate }),
      });
      if (!res.ok) throw new Error(String(res.status));
      const data = await res.json();
      setUrl(data.url); setExpiresAt(data.expiresAt);
    } catch {
      setError("生成失败，请重试");
    } finally {
      setBusy(false);
    }
  }

  async function copy() {
    try { await navigator.clipboard.writeText(url); setCopied(true); } catch { /* 用户可手动复制 */ }
  }

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/30" onClick={onClose}>
      <div className="w-[480px] rounded-xl bg-panel p-6 shadow-xl" onClick={(e) => e.stopPropagation()}>
        <h2 className="text-base font-semibold text-ink">分享预览</h2>
        <p className="mt-1 text-xs text-ink-muted">
          生成一个可分享的预览链接，无需绑定域名即可让他人查看效果。链接带「预览」水印、不被搜索引擎收录、不发送任何投放数据，默认 7 天后失效。正式投放请绑定自有域名后发布。
        </p>

        {!url ? (
          <button
            type="button"
            disabled={busy}
            onClick={() => request(false)}
            className="mt-4 rounded-md bg-brand-600 px-3 py-1.5 text-sm font-medium text-white hover:bg-brand-700 disabled:opacity-50"
          >
            {busy ? "生成中…" : "生成分享链接"}
          </button>
        ) : (
          <div className="mt-4 space-y-3">
            <div className="flex items-center gap-2">
              <input
                readOnly
                value={url}
                className="flex-1 rounded-md border border-edge bg-canvas px-2 py-1.5 text-xs text-ink"
              />
              <button type="button" onClick={copy} className="rounded-md border border-edge px-3 py-1.5 text-xs text-ink-soft hover:bg-canvas">
                {copied ? "已复制" : "复制"}
              </button>
            </div>
            {expiresAt && <p className="text-xs text-ink-muted">失效时间：{new Date(expiresAt).toLocaleString()}</p>}
            <button
              type="button"
              disabled={busy}
              onClick={() => request(true)}
              className="text-xs text-amber-700 hover:underline disabled:opacity-50"
            >
              重置链接（使已分享的旧链接立即失效）
            </button>
          </div>
        )}
        {error && <p className="mt-2 text-xs text-red-500">{error}</p>}

        <div className="mt-5 flex justify-end">
          <button onClick={onClose} className="rounded-md bg-brand-600 px-3 py-1.5 text-sm font-medium text-white hover:bg-brand-700">
            完成
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 工具栏加按钮与挂载**

`landing-editor/components/EditorToolbar.tsx`：

引入（在 `import { AntiBanPanel } ...` 下一行）：
```tsx
import { SharePreviewPanel } from "./SharePreviewPanel";
```

state（在 `const [antiBanOpen, setAntiBanOpen] = useState(false);` 下一行）：
```tsx
  const [shareOpen, setShareOpen] = useState(false);
```

按钮（在 `预览` 的 `</Link>` 之后、`<div className="relative">` 之前插入）：
```tsx
      <button
        onClick={() => setShareOpen(true)}
        className="rounded-md border border-edge px-3 py-1.5 text-sm text-ink-soft hover:bg-canvas"
      >
        分享预览
      </button>
```

挂载（在 `{antiBanOpen && <AntiBanPanel ... />}` 下一行）：
```tsx
      {shareOpen && <SharePreviewPanel onClose={() => setShareOpen(false)} />}
```

- [ ] **Step 3: lint + 类型检查**

Run: `pnpm eslint landing-editor/components/SharePreviewPanel.tsx landing-editor/components/EditorToolbar.tsx && pnpm tsc --noEmit`
Expected: 零 error。

- [ ] **Step 4: Commit**

```bash
git add landing-editor/components/SharePreviewPanel.tsx landing-editor/components/EditorToolbar.tsx
git commit -m "feat: 编辑器分享预览面板与工具栏入口"
```

---

## Task 8：端到端验证（e2e）

**Files:**
- Create: `e2e/preview-share.spec.ts`

> 依赖本地 dev server + Postgres，与既有 e2e 一致（`RUN_DB_E2E=1`）。模式参照 `e2e/gallery-and-pages.spec.ts`（Dev Login + pg 备号）与 `e2e/leads.spec.ts`。

- [ ] **Step 1: 写 e2e**

`e2e/preview-share.spec.ts`：
```ts
import { test, expect } from "@playwright/test";

// 草稿分享预览端到端：登录建页 → 生成链接 → 无会话上下文访问断言内容/水印/noindex → 重置后旧链接失效。
test.describe("草稿分享预览", () => {
  test("生成链接可匿名访问且带水印/noindex；重置后旧链接 404", async ({ page, browser, baseURL }) => {
    // 1) Dev Login
    await page.goto("/login");
    await page.getByRole("button", { name: /Dev Login/i }).click();
    await page.waitForURL(/\/admin/);

    // 2) 新建一张页面进入编辑器（画廊「空白开始」或模板均可），从 URL 取 pageId
    await page.goto("/admin/landing-pages");
    await page.getByRole("button", { name: /新建/ }).click();
    await page.getByRole("button", { name: /空白开始|直接编辑/ }).first().click();
    await page.waitForURL(/\/admin\/editor\/[0-9a-f-]+/);
    const pageId = page.url().match(/editor\/([0-9a-f-]+)/)![1];

    // 3) 生成分享链接
    await page.getByRole("button", { name: "分享预览" }).click();
    await page.getByRole("button", { name: "生成分享链接" }).click();
    const input = page.locator('input[readonly]');
    await expect(input).toHaveValue(/\/preview\//, { timeout: 10_000 });
    const firstUrl = await input.inputValue();
    const firstPath = new URL(firstUrl).pathname;

    // 4) 无会话上下文访问：应 200、含水印、robots noindex、X-Robots-Tag 头
    const anon = await browser.newContext();
    const anonPage = await anon.newPage();
    const resp = await anonPage.goto(baseURL + firstPath);
    expect(resp?.status()).toBe(200);
    expect((resp?.headers()["x-robots-tag"] ?? "")).toContain("noindex");
    await expect(anonPage.locator('meta[name="robots"]')).toHaveAttribute("content", /noindex/);
    await expect(anonPage.getByText("Made with")).toBeVisible();

    // 5) 回编辑器重置链接 → 旧链接 404
    await page.getByRole("button", { name: /重置链接/ }).click();
    await expect(input).not.toHaveValue(firstUrl, { timeout: 10_000 });
    const stale = await anon.newPage();
    const staleResp = await stale.goto(baseURL + firstPath);
    expect(staleResp?.status()).toBe(404);

    await anon.close();
  });
});
```

- [ ] **Step 2: 运行 e2e**

Run: `RUN_DB_E2E=1 pnpm exec playwright test e2e/preview-share.spec.ts`
Expected: PASS。若选择器与实际画廊按钮文案不符，按页面实际可访问名微调（优先 `getByRole`）；截图落 `.playwright`。

- [ ] **Step 3: Commit**

```bash
git add e2e/preview-share.spec.ts
git commit -m "test: 草稿分享预览端到端（匿名访问/水印/noindex/重置失效）"
```

---

## Task 9：全量验证门槛与 PR

- [ ] **Step 1: 分层验证**

Run: `pnpm vitest run && pnpm tsc --noEmit && pnpm eslint . && pnpm build 2>&1 | tail -15`
Expected: vitest 全绿（含新增 token 用例）、tsc 无错、eslint 零 error、build 成功。

- [ ] **Step 2: 更新主路线图状态**

编辑 `docs/superpowers/plans/2026-07-18-产品优先级执行路线.md`：把 Phase 1 标记为「已实现，待合并」，勾选其验收项。

- [ ] **Step 3: 推送 + PR（按护栏，首推只用 `-u origin 分支:分支`）**

```bash
git push -u origin <本分支>:<本分支>
gh pr create --base main --title "feat: 草稿分享预览链接（Phase 1）" --body "..."
```

- [ ] **Step 4: 盯 CI 全绿后交用户合并。**

---

## Self-Review

**1. Spec coverage（对照主路线图 Phase 1 范围）：**
- 平台域签名 token 预览 → Task 2/5/6 ✓
- `preview_secret` 存 DB + 轮换撤销 → Task 1/3/5 ✓
- 迁移脚本（遵循 workflow）→ Task 1 ✓
- `X-Robots-Tag` + meta noindex → Task 6（next.config 头 + generateMetadata robots）✓
- 全档强制水印 → Task 6（无条件 `<Watermark/>`）✓
- 默认 7 天过期 → Task 5（`PREVIEW_TTL_MS`）✓
- 不注入像素/追踪 → Task 6（预览页不挂 `TrackingProvider`、不发 beacon）✓
- 编辑器「分享预览」入口（生成/复制/重置）→ Task 7 ✓
- 中间件放行 `/preview/*` → Task 6 ✓
- tenant-proxy 不误判平台域 `/preview` → 平台域 `isCustomDomain=false` 直接放行，链接由 `appUrl()` 恒为平台域，无需改 tenant-proxy（已在 Architecture 说明）✓
- 首次发布引导文案 → 面板已含「正式投放请绑定自有域名后发布」；PublishDialog 大改留作可选，不阻塞 ✓
- 验收（匿名可见/带水印/noindex/过期或轮换 404/已发布自有域渲染不受影响）→ Task 8 e2e 覆盖前四项；「自有域不受影响」由未改 `/p/[slug]` + 全量 build/vitest 保障 ✓

**2. Placeholder scan：** 各步均含实际代码与命令，无 TBD/TODO。Task 8 选择器标注了「按实际文案微调」属 e2e 常规，非占位。

**3. Type consistency：** `signPreviewToken`/`verifyPreviewToken`/`decodePageId` 签名在 Task 2 定义，Task 5/6 按同签名调用（对象参数、字段名一致）；`ensurePreviewSecret`/`rotatePreviewSecret`/`getPageForPreview` 在 Task 3 定义，Task 5/6 调用一致；`previewSharePath`/`apiLandingPreviewLinkPath` 在 Task 4 定义，Task 5/6/7 使用一致。

**5. PR #30 依赖解耦：** 本分支从 `main`（尚未含 #30）建出，故 URL 拼接在 Task 5 内联 `absoluteUrl`，不 import `lib/host.appUrl`（该导出仅在未合并的 #30 上）。这样 Phase 1 与 #30 合并顺序无关、无冲突。Task 6 的预览页 import 的 `Watermark` 是 main 上的旧版（`const APP_URL`），功能正常（仅有 #30 修复的双斜杠观感问题，不影响预览）。#30 合入后可选把 `absoluteUrl` 收敛为 `appUrl`。

**4. 风险点复核：** ① token 校验先 `decodePageId` 拿 pageId 查库、再用库里 `preview_secret` 全量验签，伪造 pageId 查不到即 404；② `timingSafeEqual` 前先比长度，避免抛错；③ `AUTH_SECRET` 缺失时降级为空串仍确定性可用（生产必有），不崩溃。
