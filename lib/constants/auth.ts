export enum AuthProvider {
  Credentials = 'credentials',
  EmailOtp = 'email-otp',
  Google = 'google',
  Dev = 'dev',
}

export enum UserRole {
  USER = 'USER',
  ADMIN = 'ADMIN',
  SUPER_ADMIN = 'SUPER_ADMIN',
}
